name: AdvancedReplay
version: '${version}'
main: me.jumper251.replay.ReplaySystem
author: Jumper251
depend: [ProtocolLib]
api-version: 1.13
commands:
  replay:
    description: 主回放命令
    aliases: [rp]
    permission: replay.command

permissions:
  replay.command:
    description: 使用回放命令的权限
  replay.command.start:
    default: op
  replay.command.stop:
    default: op
  replay.command.info:
    default: op
  replay.command.play:
    default: op
  replay.command.reload:
    default: op
  replay.command.overview:
    default: op
  replay.command.list:
    default: op
  replay.command.migrate:
    default: op
  replay.command.delete:
    default: op
  replay.command.jump:
    default: op
  replay.admin:
    description: 接收更新通知的权限
